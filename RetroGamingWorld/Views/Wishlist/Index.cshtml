@model IEnumerable<RetroGamingWorld.Models.Article>

@{
    ViewData["Title"] = "Wishlist";
}

@* MESAJE DE NOTIFICARE *@
@if (TempData["message"] != null)
{
    <div class="d-flex justify-content-center align-items-center mb-5 p-4 rounded-3 shadow-sm bg-success text-white">
        @TempData["message"]
    </div>
}

@* HEADER PAGINA *@
<div class="d-flex justify-content-between align-items-center mb-5 p-4 rounded-3 shadow-sm bg-white border-start border-5 border-danger">
    <div>
        <h1 class="display-6 fw-bold mb-0">❤️ Wishlist</h1>
        <p class="text-muted mb-0">Articolele tale favorite salvate pentru mai târziu.</p>
    </div>
</div>

@* ZONA DE FILTRARE (Exact ca înainte) *@
<div class="row align-items-center mb-4 g-2">
    <div class="col">
        <form method="GET" asp-action="Index" asp-controller="Wishlist">
            <div class="input-group">
                
                <input type="text" class="form-control" 
                       placeholder="Caută în wishlist..." 
                       name="search"
                       value="@ViewBag.SearchString">

                <select name="sortCateg" class="form-select" style="max-width: 195px;">
                    <option value = "0" selected = "@(Int32.Parse(ViewBag.sortCateg) == 0)">
                        Toate consolele
                    </option>
                    @foreach(var catId in ViewBag.AllCategories)
                    {
                        <option value="@catId.Value" 
                        selected="@(ViewBag.sortCateg == @catId.Value)">
                            @catId.Text
                        </option>
                    }
                </select>

                <select name="sortBy" class="form-select" style="max-width: 100px;">
                    <option value="rating" selected="@(ViewBag.SortBy == "rating")">Rating</option>
                    <option value="price" selected="@(ViewBag.SortBy == "price")">Preț</option>
                </select>

                <select name="sortOrder" class="form-select" style="max-width: 145px;">
                    <option value="desc" selected="@(ViewBag.SortOrder == "desc")">Descrescător</option>
                    <option value="asc" selected="@(ViewBag.SortOrder == "asc")">Crescător</option>
                </select>

                <button class="btn btn-outline-danger" type="submit">Search</button>
            </div>
        </form>
    </div>

    @* PAGINARE SUS *@
    @if (Model.Any())
    {
        <div class="col-auto">
            <nav aria-label="Page navigation">
                <ul class="pagination mb-0">
                    <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                        <a class="page-link border-secondary text-dark" 
                           href="@ViewBag.PaginationBaseUrl=@(ViewBag.CurrentPage - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                    @for (int i = 1; i <= ViewBag.lastPage; i++)
                    {
                        <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                            <a class="page-link border-secondary @(i == ViewBag.CurrentPage ? "bg-danger border-danger text-white" : "text-dark")" 
                               href="@ViewBag.PaginationBaseUrl=@i">
                                @i
                            </a>
                        </li>
                    }
                    <li class="page-item @(ViewBag.CurrentPage == ViewBag.lastPage ? "disabled" : "")">
                        <a class="page-link border-secondary text-dark" 
                           href="@ViewBag.PaginationBaseUrl=@(ViewBag.CurrentPage + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
    }
</div>

@* AFISARE LISTA *@
@if (!Model.Any())
{
    <div class="d-flex justify-content-center align-items-center my-5">
        
        <div class="card shadow-sm border-0 rounded-4 p-5 text-center bg-white" 
             style="width: 100%; max-width: 600px; min-width: 300px;">
        
            <div class="mb-4">
                <div class="d-inline-flex align-items-center justify-content-center bg-danger bg-opacity-10 rounded-circle text-danger shadow-sm" 
                     style="width: 120px; height: 120px;">
                    <i class="bi bi-heart display-3"></i>
                </div>
            </div>
        
            <h2 class="fw-bold mb-3">Wishlist-ul tău este gol</h2>
        
            <p class="text-muted fs-5 mb-4">
                Nu ai salvat încă niciun articol.<br>
                Explorează colecția și salvează-ți preferatele aici!
            </p>

            <div>
                <a asp-controller="Articles" asp-action="Index" class="btn btn-danger btn-lg px-5 rounded-pill shadow-sm">
                    Mergi la articole
                </a>
            </div>
        </div>
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mb-5">
        
        @foreach (var article in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm border-0 card-hover-effect position-relative">
                    
                    @* 1. Badge Categorie *@
                    @if(article.Category != null)
                    {
                        <span class="position-absolute top-0 end-0 badge bg-danger m-3 shadow-sm" style="z-index: 10;">
                            @article.Category.CategoryName
                        </span>
                    }

                    @* 2. Imagine *@
                    <div class="card-img-top bg-light d-flex align-items-center justify-content-center position-relative" style="height: 300px; overflow: hidden;">
                        @if (!string.IsNullOrEmpty(article.Image))
                        {
                            <img src="@article.Image" class="w-100 h-100" style="object-fit: cover;" alt="@article.Title">
                        }
                        else
                        {
                            <span class="text-muted">Fără Imagine</span>
                        }
                    </div>

                    @* 3. Corp Card *@
                    <div class="card-body">
                        <h5 class="card-title fw-bold text-dark mt-2 text-truncate">@article.Title</h5>
                        
                        <p class="card-text small mb-1">
                            De: <span class="fw-semibold text-danger">@(article.User?.UserName ?? "Necunoscut")</span>
                        </p>

                        @* Rating *@
                        <div class="d-flex align-items-center mb-2">
                                <div class="d-flex text-warning small">
                                    @{
                                        float ratingVal = article.Rating ?? 0;
                                        for (int i = 1; i <= 5; i++)
                                        {
                                            if (ratingVal >= i)
                                            {
                                                <i class="bi bi-star-fill"></i>
                                            }
                                            else if (ratingVal >= i - 0.5)
                                            {
                                                <i class="bi bi-star-half"></i>
                                            }
                                            else
                                            {
                                                <i class="bi bi-star text-secondary opacity-25"></i>
                                            }
                                        }
                                    }
                                </div>
                                <span class="fw-bold text-muted ms-2 small">
                                    @(ratingVal.ToString("0.0")) <span class="fw-light">/ 5.0</span>
                                </span>
                            </div>
                        
                        @* Descriere *@
                        <p class="card-text text-muted small" style="height: 40px; overflow: hidden;">
                            @if (!string.IsNullOrEmpty(article.Content) && article.Content.Length > 80)
                            {
                                @article.Content.Substring(0, 80) <span class="text-danger">...</span>
                            }
                            else
                            {
                                @article.Content
                            }
                        </p>
                    </div>

                    @* 4. Footer Pret/Stoc *@
                    <div class="card-footer bg-white border-top-0 d-flex justify-content-evenly align-items-center pb-3">
                        <p class="card-text small mb-1">
                            <span class="fw-semibold text-danger">@article.Price.ToString()</span> RON
                        </p>
                        <p class="card-text small mb-1">
                            Stoc: <span class="fw-semibold text-danger">@article.Stock.ToString()</span>
                        </p>
                    </div>

                    @* 5. Footer Data *@
                    <div class="card-footer bg-white border-top-0 d-flex justify-content-between align-items-center pb-3">
                        <small class="text-muted">
                            📅 @article.Date.ToString("dd MMM")
                        </small>

                        <div class="btn-group shadow-sm" role="group">
                            
                            @* Buton Detalii *@
                            <a href="/Articles/Show/@article.Id" class="btn btn-sm btn-outline-secondary" title="Vezi detalii">
                                <i class="bi bi-eye"></i>
                            </a>

                            @* Buton Adauga in Cos *@
                            @if (article.Stock > 0)
                            {
                                <form method="post" asp-controller="Cart" asp-action="Add" asp-route-articleId="@article.Id" class="d-inline">
                                    <button type="submit" class="btn btn-sm btn-success" style="border-radius: 0;" title="Mută în coș">
                                        <i class="bi bi-cart-plus"></i>
                                    </button>
                                </form>
                            }
                            else
                            {
                                <button class="btn btn-sm btn-outline-secondary disabled" style="border-radius: 0;" title="Stoc Epuizat">
                                    🚫
                                </button>
                            }

                            @* Buton Sterge din Wishlist *@
                            <form method="post" asp-controller="Wishlist" asp-action="Remove" asp-route-articleId="@article.Id" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger" title="Șterge din wishlist">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                            
                        </div>
                    </div>

                </div>
            </div>
        }
    </div>
}

@* PAGINARE JOS *@
@if (Model.Any() && ViewBag.lastPage > 1)
{
    <div class="d-flex justify-content-center mt-2 mb-5 text-muted small">
        <p>Pagina @ViewBag.CurrentPage din @ViewBag.lastPage</p>
    </div>
}